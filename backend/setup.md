
```
frac-vizzy (main) $ cd backend

backend (main) $ python -m venv venv

backend (main) $ source venv/bin/activate

(venv) backend (main) $ pip install pip-tools

(venv) backend (main) $ pip install -r requirements.txt
```
